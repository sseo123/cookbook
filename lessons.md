1. boilerplate:
   the skeleton of code that can be used over and over again
2. end points (refering to api):
   when you search something up, under the hood, you are sending a request to the endpoint, but the search query is equal to what you serched. for example, if you search up chicken, you are sending a req for chicken, but it will look something like (api/json/sda/searchsapod=chicken)
3. What's an API?
   API allows two programs to talk to eachother. Allows you to get or send data between your app and another serivce
4. <p>:
   when you want to include text you can <p> which is short for paragraph
5. css, rem:
   is the y x paddings, and rem is short for room em. em is a unit relative to font size of the parent element, rem is a relative unit of measurement that is base on the font size of the coduments root element (<html> tag)
6. for script.js, I only knew the correct url because of the documentation page at https://www.themealdb.com/api.php

7. typos aren't accounted for, so make sure to be very aware of this
